# Core scientific stack
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Bayesian optimization
optuna>=3.6.0

# Simulated Annealing backend (D-Wave)
dwave-neal>=0.6.0

# Optional: progress bars
tqdm>=4.65.0

# Note: dwave-ocean-sdk is required for QPU execution via D-Wave Leap.
# QPU runs are only available from within the official QuantumCLEF workspace.
# dwave-ocean-sdk>=7.0.0
